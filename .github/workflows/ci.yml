name: Test CI

on: [push]

jobs:
  my-job:
    runs-on: edu-self-hosted
    timeout-minutes: 10
    services:
      docker-dind:
        image: docker:dind
        privileged: true
        env:
          DOCKER_TLS_CERTDIR: ""
    container:
      image: ubuntu:latest
    steps:
      - name: Install Docker CLI
        run: |
          apt-get update
          apt-get install -y docker.io
          docker --version
          docker version
